import 'package:flutter/material.dart';

class AnimacaoExplicita extends StatelessWidget {
  const AnimacaoExplicita({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
